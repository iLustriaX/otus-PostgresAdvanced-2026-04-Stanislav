# Создание виртуальной машины

Создаю ВМ аналогичную ВМ из 1-го ДЗ.

# Установка Docker

Обновляю пакеты:
```bash
sudo apt update && sudo apt upgrade -y
```

Устанавливаю docker:
```bash
sudo apt install -y docker.io
```

Добавляю docker в автозагрузку:
```bash
sudo systemctl enable --now docker
```

Добавляю пользователя в группу docker, чтобы не вводить sudo:
```bash
sudo usermod -aG docker $USER
```

`-a` - означает, что группу нужно добавить к списку групп пользователя, а не заменить текущий список.
`-G` - сообщает, чтобы следующий аргумент это имя группы.

Перезахожу в сессию.

Проверка:
```bash
docker run hello-world
```

```bash
Unable to find image 'hello-world:latest' locally
latest: Pulling from library/hello-world
4f55086f7dd0: Pull complete
d5e71e642bf5: Download complete
Digest: sha256:96498ffd522e70807ab6384a5c0485a79b9c7c08ca79ba08623edcad1054e62d
Status: Downloaded newer image for hello-world:latest

Hello from Docker!
This message shows that your installation appears to be working correctly.
```
# Установка kubectl

Скачиваю бинарник:
```bash
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
```

`-L` - следовать HTTP-редиректам.
`-O` - сохранить файл с именем из URL
`-s` - тихий режим. Отобразить только вывод файла stable.txt

Даю права на выполнение и переношу в директорию bin:
```bash
chmod +x kubectl
sudo mv kubectl /usr/local/bin/
```

Проверяю работоспособность:
```bash 
kubectl version --client
```
```
Client Version: v1.36.2
Kustomize Version: v5.8.1
```

# Установка Minikube

Устанавливаю minikube:
```bash
curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
sudo install minikube-linux-amd64 /usr/local/bin/minikube
```

Проверяю работоспособность:
```bash
minikube version
```
```bash
minikube version: v1.38.1
commit: c93a4cb9311efc66b90d33ea03f75f2c4120e9b0
```

# Установка psql

Устанавливаю psql:
```bash
sudo apt install -y postgresql-client
```

Проверяю работоспособность:
```bash
psql --version
```
```
psql (PostgreSQL) 16.14 (Ubuntu 16.14-0ubuntu0.24.04.1)
```

# Установка Helm

Скачиваю скрипт установки Helm:
```bash
curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3
```
`-f` - при ошибке HTTP (например, 404 или 500) `curl` вернёт код ошибки и не создаст файл.
`-S` - (используется вместе с `-s`) — если произошла ошибка, показать сообщение об ошибке (иначе при `-s` вообще ничего не видно). Пара `-sS` — стандартный «тихий, но с ошибками» режим.
`-o` - сохранить результат в указанный файл.

Т.к. не работает из России. Скачиваю через VPN локально и переношу в облако.

Запускаю скрипт установки:
```bash
chmod 700 get_helm.sh
./get_helm.sh
```

Проверяю работоспособность:
```bash
helm version
```
```
version.BuildInfo{Version:"v3.21.3", GitCommit:"1ad6e68924fdf6fb0c7dcef8e9e1dfc0f36eaed6", GitTreeState:"clean", GoVersion:"go1.26.5"}
```

# Запуск minikube

Запуск minikube через драйвер `docker`:
```bash
minikube start --driver=docker
```

Проверяю состояние запуска:
```bash
kubectl cluster-info
```
```
Kubernetes control plane is running at https://192.168.49.2:8443
CoreDNS is running at https://192.168.49.2:8443/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy

To further debug and diagnose cluster problems, use 'kubectl cluster-info dump'.
```

```bash
kubectl get nodes
```
```
NAME       STATUS   ROLES           AGE   VERSION
minikube   Ready    control-plane   47s   v1.35.1
```

# PostgreSQL Deployment + Service

`postgres-deployment.yaml`:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "testuser"
            - name: POSTGRES_PASSWORD
              value: "testpass123"
            - name: POSTGRES_DB
              value: "testdb"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
```

Применяю манифест:
```bash
kubectl apply -f postgres-deployment.yaml
```

Жду некоторое время и проверяю запуск пода:
```bash
kubectl get pods
```
```
NAME                                   READY   STATUS    RESTARTS   AGE
postgres-deployment-865d8bb5b4-jhqsq   1/1     Running   0          64s
```

Включаю временный проброс портов на машину:
```bash
kubectl port-forward service/postgres-service 5432:5432 &
```

Выполняю `SELECT` запрос к PostgreSQL в deployment:
```bash
psql -h localhost -p 5432 -U testuser -d testdb -c "SELECT 'Hello from PostgreSQL' AS message;"
```
```
Handling connection for 5432
        message
-----------------------
 Hello from PostgreSQL
(1 row)
```

Удаляю ресурсы:
```bash
kubectl delete -f postgres-deployment.yaml
```

Отключаю проброс портов:
  ```bash
  jobs
  kill %1
  ```
# Helm чарт

Добавляю репозиторий Bitnami с зеркала yandex:
```bash
helm repo add bitnami https://mirror.yandex.ru/helm/charts.bitnami.com
helm repo update
```

Создаю файл конфигурации `values.yaml`:
```yaml
auth:
  postgresPassword: "securepass456"
  username: "appuser"
  database: "appdb"

replicaCount: 1

persistence:
  enabled: false

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"
```

Устанавливаю чарт:
```bash
helm install my-postgres bitnami/postgresql -f values.yaml
```

Для удаления:
```
helm uninstall my-postgres
```